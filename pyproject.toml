[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "policyengine"
version = "5.3.1"
description = "A package to conduct policy analysis using PolicyEngine tax-benefit models."
readme = "README.md"
authors = [
    {name = "PolicyEngine", email = "hello@policyengine.org"},
]
license = {file = "LICENSE"}
requires-python = ">=3.11,<3.15"
classifiers = [
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "pydantic>=2.0.0",
    "pandas>=2.0.0",
    "h5py>=3.0.0",
    "microdf_python>=1.3.0,<1.4",
    "jsonschema>=4.0.0",
    "requests>=2.31.0",
    "psutil>=5.9.0",
    "packaging>=23.0",
]

[project.scripts]
policyengine = "policyengine.cli:main"

[project.optional-dependencies]
plotting = [
    "plotly>=5.0.0",
]
graph = [
    "networkx>=3.0",
]
models = [
    "policyengine-core==3.30.1",
    "policyengine-us==1.764.6",
    "policyengine-uk==2.90.2",
    "spm-calculator==0.3.1",
]
uk = [
    "policyengine-core==3.30.1",
    "policyengine-uk==2.90.2",
]
us = [
    "policyengine-core==3.30.1",
    "policyengine-us==1.764.6",
    "spm-calculator==0.3.1",
]
dev = [
    "pytest",
    "furo",
    "autodoc_pydantic",
    "jupyter-book",
    "yaml-changelog>=0.1.7",
    "itables",
    "build",
    "plotly>=5.0.0",
    "pytest-asyncio>=0.26.0",
    "ruff>=0.9.0",
    "towncrier>=24.8.0",
    "mypy>=1.11.0",
    "pytest-cov>=5.0.0",
    "policyengine-core==3.30.1",
    "policyengine-us==1.764.6",
    "policyengine-uk==2.90.2",
    "spm-calculator==0.3.1",
]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"policyengine" = ["**/*"]

[tool.setuptools.exclude-package-data]
"policyengine" = ["**/__pycache__/*", "**/*.pyc", "**/*.pyo"]

[tool.pytest.ini_options]
addopts = "-v"
testpaths = [
    "tests",
]
filterwarnings = [
    "ignore::pydantic.warnings.PydanticDeprecatedSince20",
]

[tool.ruff]
line-length = 88
target-version = "py311"
extend-exclude = ["*.ipynb"]

[tool.ruff.lint]
select = [
  "E",  # pycodestyle errors
  "F",  # pyflakes
  "I",  # import sorting
  "W",  # pycodestyle warnings
  "UP", # pyupgrade
]
ignore = [
  "E501",  # Ignore line length errors
  # Preserve the existing annotation style across Pydantic models.
  "UP006",
  "UP007",
  "UP035",
  "UP045",
  "UP042",  # Preserve public Enum string representations.
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.towncrier]
package = "policyengine"
directory = "changelog.d"
filename = "CHANGELOG.md"
title_format = "## [{version}] - {project_date}"
issue_format = ""
underlines = ["", "", ""]

[[tool.towncrier.type]]
directory = "breaking"
name = "Breaking changes"
showcontent = true

[[tool.towncrier.type]]
directory = "added"
name = "Added"
showcontent = true

[[tool.towncrier.type]]
directory = "changed"
name = "Changed"
showcontent = true

[[tool.towncrier.type]]
directory = "fixed"
name = "Fixed"
showcontent = true

[[tool.towncrier.type]]
directory = "removed"
name = "Removed"
showcontent = true
