Add the weighted-module design note: numpy kernels with a parity test against microcosm-frame, weights as a named column, and a closed `df.weighted(...)` accessor replacing the microdf dependency (#528).
